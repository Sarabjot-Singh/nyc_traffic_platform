datasets:
  tbl_yellow_trips_dashboard:
    enabled: true

    module: src.gold.tbl_yellow_trips_dashboard.tbl_yellow_trips_dashboard
    class_name: TblYellowTripsDashboard

    format: delta

    partition_columns:
      - activity_date