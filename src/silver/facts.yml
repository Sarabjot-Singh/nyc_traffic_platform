facts:
  fact_yellow_trip:
    enabled: true

    depends_on:
      bronze:
        - yellow_trip
      silver:
        dimensions:
          - dim_vendor
          - dim_rate_code
          - dim_payment_method
          - dim_location

    module: src.silver.yellow_trip.fact_yellow_trip
    class_name: FactYellowTrip

    format: delta

    partition_columns:
      - partition_day