facts:
  fact_yellow_trip:
    name: fact_yellow_trip
    module: fact_yellow_trip
    module_path: src.silver.yellow_trips.fact_yellow_trip
    class: FactYellowTrip
    load_method: merge
    table_type: DeltaLake
    enabled: True
    partition_by: pickup_datetime # This is just a metadata column to know on which column partition column is based on
    partition_column:
      - partition_day